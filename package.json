{
  "name": "crew-ops-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "CrewOps Recovery Copilot — deterministic airline crew disruption analysis with an optional LLM planner",
  "type": "module",
  "scripts": {
    "api": "tsc --declaration false && node dist/src/api/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval:agent": "tsc --declaration false && node dist/evals/runAgentEval.js",
    "eval:agent:live": "tsc --declaration false && node dist/evals/runAgentEval.js --live"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
