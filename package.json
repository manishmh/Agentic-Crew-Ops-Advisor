{
  "name": "crew-ops-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "dCortex Agentic Crew Ops Advisor — Step 1: canonical domain model, loader, in-memory operational graph",
  "type": "module",
  "scripts": {
    "api": "tsc --declaration false && node dist/src/api/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
